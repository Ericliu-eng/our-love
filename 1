<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>我们的恋爱时光</title>
    <style>
        body {
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background: #fffafa; /* 淡粉白背景 */
            font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
        }

        .container {
            text-align: center;
            background: white;
            padding: 2rem;
            border-radius: 30px;
            box-shadow: 0 10px 30px rgba(255, 182, 193, 0.3);
            border: 1px solid #ffe4e1;
            max-width: 400px;
            width: 90%;
        }

        .heart {
            font-size: 40px;
            color: #ff6b6b;
            margin-bottom: 10px;
        }

        h2 {
            color: #555;
            font-weight: 300;
            margin-bottom: 20px;
        }

        .timer-display {
            background: #fff0f0;
            padding: 20px;
            border-radius: 20px;
            margin-bottom: 20px;
        }

        .days {
            font-size: 3rem;
            font-weight: bold;
            color: #ff6b6b;
            display: block;
        }

        .units {
            font-size: 1rem;
            color: #888;
            margin-top: 10px;
        }

        .footer-date {
            font-size: 0.85rem;
            color: #bbb;
            letter-spacing: 1px;
        }
    </style>
</head>
<body>

<div class="container">
    <div class="heart">❤️</div>
    <h2>我们相爱已经</h2>
    
    <div class="timer-display">
        <span id="days-val" class="days">0</span>
        <div id="sub-val" class="units">00 小时 00 分 00 秒</div>
    </div>

    <div class="footer-date">SINCE 2025.12.07</div>
</div>

<script>
    function countTime() {
        // 设置恋爱起始时间
        const startTime = new Date('2025-12-07T00:00:00').getTime();
        const now = new Date().getTime();
        const diff = now - startTime;

        // 计算各项数值
        const d = Math.floor(diff / (1000 * 60 * 60 * 24));
        const h = Math.floor((diff / (1000 * 60 * 60)) % 24);
        const m = Math.floor((diff / (1000 * 60)) % 60);
        const s = Math.floor((diff / 1000) % 60);

        // 更新界面
        document.getElementById('days-val').innerText = d + " 天";
        document.getElementById('sub-val').innerText = 
            `${h < 10 ? '0' + h : h} 小时 ${m < 10 ? '0' + m : m} 分 ${s < 10 ? '0' + s : s} 秒`;
    }

    // 每秒刷新一次
    setInterval(countTime, 1000);
    countTime();
</script>

</body>
</html>
